/**
 * Space Object Description Data XML (SODDXA).
 *
 * <p>This package provides the top level XML handling for the SODDX TRE body content.
 *
 * <p>The main classes are {@link SpaceObjectDescriptionData} and {@link
 * SpaceObjectDescriptionDataParser}. {@code SpaceObjectDescriptionData} is the the top level POJO,
 * and {@code SpaceObjectDescriptionDataParser} provides the serialisation and deserialisation of
 * that {@code SpaceObjectDescriptionData} to and from XML.
 */
package net.frogmouth.rnd.jim.nitf.tre.soddxa.xml;
